// import axios from 'axios'

// const host = axios.create({
//   baseURL: process.env.REACT_APP_API_URL
// })

// export const hello = async () => {
//   const response = await host.get('hello')
//   return response.data
// }

// export const getBasket = async (id) => {
//   const response = await host.get('basket', { params: { id } })
//   return response.data
// }

// export const saveBasket = async (id, basket) => {
//   const response = await host.post('basket', { basket }, { params: { id } })
//   return response.data
// }
